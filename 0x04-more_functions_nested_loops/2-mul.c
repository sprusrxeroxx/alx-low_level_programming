#include "main.h"

/**
 * _mul - multiplies 2 intergers
 * @a : first integer
 * @b : second integer
 * Return: always 0
 */

int mul(int a, int b);
{
	_putchar("%d\n", mul(2, 8));
	_putchar("%d\n", mul(222, 1));

	return (0);
}
